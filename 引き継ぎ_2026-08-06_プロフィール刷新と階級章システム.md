# 引き継ぎ書：プロフィール刷新＋宇宙階級章システム（2026-08-06）

対象リポジトリ：`rediance-sns`／本番モック＝`app.html`
**この文書は「実装の地図」。最終的な正は常に `app.html` のコードそのもの。** 数値・パスがこの文書とズレたらコードが正しい。文書内の grep キーで該当箇所に飛べる。

前回引き継ぎ（0f9ea74・ブランドカラー刷新）以降の変更を全て記載する。

---

## 0. ブランド名の変更

- サービス名は **HYPERAY → CONSEQ** に改名済み。`<title>`・ヘッダーロゴ・文中の旧名は全て置換済み
- 内部コードネームは従来通り Rediance（リポジトリ名・ディレクトリ名）
- ロゴ検討の経緯は `mockups/logo-conseq*.html`（5案・記録用）

---

## 1. プロフィール画面（自分・他人共通の新構造）

現行インスタグラム（2026年iOS版）のプロフィール構造の完コピ。**バナー画像は廃止**（旧・階級の空バナー／写真バナーとも使わない）。

### 1-1. 構造（上から順）

```
[白トップバー]
  自分プロフ： @taisho_k（黒 bold 20px・中央）＋☰（右・openSettingsMenu）※戻る「‹」なし（タブ画面のため）
  他人プロフ： ←（戻る）＋ @handle（bold・黒）   ※表示名ではなく@ハンドルを出す（IG準拠）
[ヘッダー行]（display:flex・gap 16px・padding 16px）
  アバター 88px 円（薄グレー1px リング＋2px 内側余白。他人は profilePhoto があれば写真、
  なければ avatarEmoji＋avatarClass グラデ円）
  右カラム（縦積み）：
    名前 16px / font-weight 600
    スタッツ3枠（横並び・各 flex:1・各枠内センター揃え）：
      枠1 = 階級章（下記 §3。裸のグラデSVG章 24px）／下段ラベル = 階級名 13px グラデ文字
      枠2 = スコア 18px bold（K/M表記）／下段 = 「影響力」13px グレー(#6B7280系)
      枠3 = 前日比 18px bold（上昇 ▲ 緑 #10B981／下降 ▼ 赤 #EF4444）／下段 = 「前日比」13px グレー
      ※3枠の上段は統一高 24px・display:flex・align-items:center（ラベルの縦位置を揃えるため必須）
[bio ブロック]（全幅・左右 16px）
  自分プロフ： カテゴリ行 13px グレー「経営者・AIテック」＋本文 14px「シンガポール🇸🇬 ↔ 日本🇯🇵で活動中」
  他人プロフ： bio 1行 14px のみ（カテゴリ行なし）
[ボタン行]  高さ34px・角丸8px・14px semibold・グレー地（IGボタン寸法）
  自分： Edit Profile / Circle Settings / Group Settings（onclick は従来機能のまま）
  他人： Follow / メッセージ / ＋（従来のまま）
[タブ]  Posts / Identity（.my-profile-tab-bar・従来のまま）
```

grep キー：`.ig-top-bar` `.ig-header-row` `.ig-stats` `.ig-stat-value` `.ig-bio-block`／自分プロフ=`screen-my`／他人=`buildPokemonCard`

### 1-2. 前日比（trend）データ

- `userProfiles` の各ユーザーに `trend` フィールド（数値%・負あり）を追加済み
- 未設定ユーザーのフォールバック：`userTrendPct(user)` ＝ `Math.max(-15, Math.min(15, (score % 23) - 5))`（決定的・毎回同値）
- 自分プロフは静的「▲ +12%」

### 1-3. 撤去したもの

- **4指標行（💖Trust/💎Prestige/📝Insights/🏛️Stature）は自分・他人とも撤去**。ただし `STAT_INFO` オブジェクト・`showStatInfo()` は温存（将来のスコア詳細シートで再利用予定）
- 旧ポケモンカード型の他人プロフヘッダー・旧 `.insta-rs-block`・階級ピルバッジ（`.rank-badge-star` 等の使用箇所）・階級の空バナー（`tierSkyBg`/`STAR_BG_URI_PLAIN`。関数定義はデッドコードとして残置）

---

## 2. 宇宙階級の表示システム（共通ルール）

10ティア・閾値は `getTitle(score)` が唯一の正（Universe≥500万 … Human 0。変更なし）。

### 2-1. ティアグラデ（CSS：`.rs-tier-grad` ＋ `.rs-tg-*`）

文字用。`background-image:` ＋ `background-clip:text` ＋ `-webkit-text-fill-color:transparent`。

| tier | linear-gradient(135deg, …) |
|---|---|
| human | #4B5563 → #6B7280 |
| leader | #92400E → #F59E0B |
| icon | #047857 → #34D399 |
| rising | #0D9488 → #84CC16 |
| star | #1E40AF → #06B6D4 |
| nova | #6D28D9 → #C026D3 |
| supernova | #BE123C → #F97316 |
| gravity | #312E81 → #0D9488 |
| singularity | #0F172A → #7C3AED |
| universe | #1A2E05 0% → #84CC16 38% → #0D9488 65% → #4C1D95 100%（ブランドグラデL-4と同一） |

### 2-2. ティア明色（TIER_BRIGHT・暗い地の文字用ソリッド）

human #9CA3AF / leader #F59E0B / icon #34D399 / rising #84CC16 / star #06B6D4 / nova #C026D3 / supernova #F97316 / gravity #2DD4BF / singularity #8B5CF6 / universe #84CC16

### 2-3. 階級名テキストの表記統一

- 全箇所「大文字TIERNAME・weight 800」（例 RISING・SUPERNOVA）
- 明るい地（白地・月光帯）＝ティアグラデ文字／暗い地（色帯）＝TIER_BRIGHT ソリッド文字
- 明暗判定 `cpBgIsLight(bgId)`（月光=gekko のみ明るい扱い）
- 適用済み：タイムライン帯・ストーリービューア・コメント・ストーリー一覧・メンバー選択リスト・グループ作成/編集メンバー行・通知文中
- **未適用（絵文字のまま・意図的スコープ外）**：イベント招待帯・参加者シート・検索カード・イベント詳細ホストバッジ

---

## 3. 階級章（メゾン10章・SVG）

絵文字バッジは全廃し、オリジナルSVG章「メゾン・コスミック改（第3案）」を採用。

### 3-1. 資産の場所

- **正＝`app.html` の `<body>` 直後のスプライト** `<svg width="0" height="0" style="position:absolute" aria-hidden="true"><defs>…</defs></svg>`
  - `shape-{tier}`（シルエット `<g>`・fill="currentColor"）×10
  - `mask-{tier}`（アルファマスク）×10
  - `gradient-{tier}`（linearGradient・§2-1と同値）×10
- 設計原典（検討記録）：`mockups/badge-maison-v3.html`（＋RISING検討の変遷は `mockups/rising-*.html`）
- デザイン原則：主役ひとつの幾何・要素2〜3個・ハイジュエリー的品格。RISING は雫形で確定

### 3-2. 余白切り詰め（重要）

各章の外接枠を実測し、**タイトな viewBox** で描画する（指定サイズ＝視覚サイズにするため。章ごとの見かけサイズのムラと文字整列ズレを防ぐ）。JS定数 `TIER_BADGE_BOX`：

```
human 3.45 2.90 17.10 18.75 / leader 3.15 1.05 17.71 20.80 / icon 1.15 4.35 21.70 15.30
rising 3.55 1.05 16.90 21.95 / star 1.75 1.05 20.50 19.50 / nova 0.75 0.75 22.50 22.50
supernova 0.45 0.45 23.10 23.10 / gravity 1.25 1.25 21.50 21.50
singularity 1.35 1.40 21.30 21.20 / universe 0.10 0.10 23.80 23.80
```

### 3-3. 描画ヘルパー

```js
tierBadgeSvg(score, size)
// → <svg width=size height=size viewBox=TIER_BADGE_BOX[cls] preserveAspectRatio="xMidYMid meet">
//     <rect width="24" height="24" fill="url(#gradient-cls)" mask="url(#mask-cls)"/></svg>
```

### 3-4. 使用場所

- **プロフィールのスタッツ枠のみ**：裸のグラデ章 24px（白地なので台座なしの「刻印」スタイル）。自分プロフは静的SVG直書き・他人は `tierBadgeSvg(user.score, 24)`

---

## 4. エナメルバッジ（タイムライン・コメント用）

色付き帯の上での可読性問題の最終解。**ティア色グラデの円台座＋白抜き章＋極細白縁＋外側の極薄墨シャドウ**（エナメル勲章様式）。台座が帯色を受け止めるため**帯の明暗分岐は不要（全帯共通）**。

### 4-1. 資産の場所

- **正＝`app.html` スプライト内**：
  - `enamel-{tier}`（台座用グラデ×10。**§2-1の色をそのまま使わず、白章とのコントラスト基準で明度補正済み**。例 star #2563a8→#155e75・supernova #be3655→#9a3412。正確な値はスプライト参照）
  - `mark-{tier}`（白抜き用に小サイズ調整済みの章シルエット×10。細部省略・白面積を揃えた別彫り）
  - `badge-{tier}`（台座円＋白縁＋mark を組んだ完成品 symbol・viewBox 0 0 48 48）×10
  - `#ink-shadow`（墨シャドウ filter）
  - 光源は全ティア左上統一。SINGULARITY・UNIVERSE のみ白縁の内側に極細乳白リング（最上位の構造差）
- 設計原典：`mockups/badge-enamel.html`

### 4-2. 描画ヘルパーと使用場所

```js
tierEnamelSvg(score, size)
// → <svg width=size height=size viewBox="0 0 48 48"><use href="#badge-cls"/></svg>
```

- **タイムライン帯（cpBandHtml）＝20px**・**コメント行（postThread）＝16px**。名前の右に配置、その右に階級名テキスト（§2-3のルールのまま）
- プロフィール・ストーリービューアには使わない（プロフ＝裸章／ビューアは文字のみ）

### 4-3. 使い分けの思想（そのまま伝えてよい）

- **プロフィール＝白地の「刻印」**（裸のグラデ章。ジュエリーの彫り）
- **タイムライン・コメント＝「勲章」**（エナメルバッジ。どの帯色でも浮く）
- 章のシルエットは共通なので、ユーザーには同一の階級記号として認知される

---

## 5. ストーリービューアの動線修正

- ヘッダーの**アバターと名前ブロックにタップを追加**：`event.stopPropagation(); closeCpStories(); openUserProfile(uid)`
- `openUserProfile` は自分（taisho_k）の場合の分岐を内蔵済みなので特別処理不要
- grep キー：`cpStoryRender` 内 `cpStoryHeader`

---

## 6. 実装の罠（再現時に必ず守ること）

1. **グラデ文字は `background-image:` で書く**。`background:` ショートハンドは `background-clip:text` をリセットし、文字が塗り潰しバーになる
2. **SVGスプライトは `width="0" height="0" style="position:absolute"`**。`display:none` だと mask/gradient が効かない環境がある
3. **data-URI を HTML の style="" 属性に埋める場合はシングルクォート包み** `url('data:…')`。ダブルクォートは属性を分断する
4. **絵文字をグラデ文字 span に入れると単色シルエット化する**（background-clip の仕様）。本システムでは絵文字バッジは廃止済みなので通常は関係ないが、文中絵文字（§2-3の未適用箇所）を触る際は注意
5. app.html は複数 `<script>` ブロック構成。**関数は呼び出し元と同じブロックに置く**（hoisting はタグを跨がない）
6. スタッツ3枠の上段は**統一高24px の flex センター**にしないとラベル位置がズレる（§1-1）

---

## 7. 検証チェックリスト（再現後にこれを全部確認）

- [ ] 自分プロフ：白バー @taisho_k・アバター88px・「木村 誠司」16px・裸のSTAR章24px＋STARグラデ文字・50.9K・▲+12%・ラベル3つの縦位置が揃う
- [ ] 他人プロフ：トップバーが @handle・写真アバター（rina_s）／絵文字アバター（universe_zero）両対応・前日比マイナスが赤（sho_n ▼-2%）
- [ ] タイムライン：全帯でエナメル20px（月光・暁・星雲・星空で白縁が浮く）・階級名は明帯グラデ／暗帯明色
- [ ] コメント：エナメル16px＋階級名
- [ ] ストーリービューア：名前タップ→ビューアが閉じてその人のプロフが開く
- [ ] 階級章10種：HUMAN〜UNIVERSE が §3-2 の比率で視覚サイズが揃っている
- [ ] JSコンソールエラーなし

---

## 8. 今回スコープ外（未着手・保留）

- 通知・イベント関係・検索カード等の絵文字ティア表示のエナメル化（§2-3の未適用箇所）
- 階級・影響力タップで開く説明シート（階級ラダー＋スコアの仕組み。方向のみ合意・未着手）
- RS計算式（カスケード拡散モデル）・シーズン制ティア焼き付けは設計継続中＝スコアは全ユーザー手打ちのまま
- デッドコード（旧ポケモンカードCSS・階級の空バナー関数群・upx-banner系CSS）は意図的に残置。掃除は別途指示があるまで触らない
