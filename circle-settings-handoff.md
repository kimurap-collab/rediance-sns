# Circle Settings 本番実装 引き継ぎ書

- 最終更新: 2026-08-12
- 対象読者: 本番アプリを実装するエンジニア／その補助AI
- プロダクト: **CONSEQ**（影響力SNS・内部コードネーム Rediance）
- リポジトリ: https://github.com/kimurap-collab/rediance-sns

---

## 1. 概要（Circle Settings とは）

Circle Settings は、ユーザーが **コネクト（相互の関係）した相手を「心理的な距離」で5階層に配置・管理する画面**。

CONSEQ はフォロワー"数"ではなく **関係の質・影響力** を可視化するSNS。その中核として、コネクト相手を「どのくらい近い関係か」で仕分けるのがこの画面。配置は本人だけの私的な整理であり、相手には見えない（後述）。

本書は、モック（見本HTML）で確定した仕様と、**本番実装で必ず対応すべき申し送り**をまとめたもの。

---

## 2. 現物の場所（まずここを見る）

| 種別 | 場所 | 備考 |
|---|---|---|
| 見本（動く現物・デザイン確定版） | `mockups/circle-settings-v5.html` | 単一HTML。ブラウザで直接開ける |
| 本番モック本体 | `app.html` | プロフィール →「Circle Settings」ボタン、または設定メニュー ⚙️ から遷移 |

本番は、**見本v5の見た目・挙動を、実データと本番フレームワークで再現**するのがゴール。

---

## 3. 画面構成：2タブ

上部に固定ヘッダー（`← 戻る ｜ Circle Settings`）、その下に2タブ。

- **メンバー** タブ … コネクト済みの全員を階層別に一覧・管理
- **新規配置** タブ … まだ階層に振り分けていない新規コネクトを一気に配置

新規配置タブには、未配置の件数を赤バッジで表示（例: `20`）。プロフィール上の入口ボタンにも同じ赤バッジを出し、「新しいコネクトが来ている」ことに気づかせる。

---

## 4. 階層モデル（距離軸・最重要）

コネクト相手を「距離」で5階層に置く。**縦の優劣ではなく、同心円状の"距離の種類"**という思想。

| 表示名 (UI) | 内部キー (code) | 枠上限 | 意味 |
|---|---|---|---|
| **Inner** | `core` | 10 | 非常に近い関係 |
| **Near** | `orbit` | 15 | 親しい関係 |
| **Mid** | `halo` | 50 | ゆるやかな関係 |
| **Wide** | `galaxy` | 150 | 広い関係 |
| **Connect** | `connect` | 500 | 認識しあう関係 |

### 実装上の最重要注意
- **UI表示名（Inner…）と内部キー（core…）が違う。** 見本コードは内部キーを旧名 `core/orbit/halo/galaxy/connect` のまま保持し、`tiers[key].label` で表示名を出している。本番でもこの対応表を**1箇所に集約**し、DB・API・UIで崩さないこと。
- **改名の背景（誤実装防止のため必読）**: 旧称は Core / Orbit / Halo / Galaxy で、"Coreが頂点"という縦社会イメージがあった。これを捨て、**Inner / Near / Mid / Wide という距離軸**に改名。コネクトした時点で全員が重要な関係であり、階層は優劣ではなく「距離の種類」。近い関係ほど少人数（枠が小さい）なのは希少性として自然。
- 枠数（10/15/50/150/500）は **ダンバー数** を参照。**枠の合計値はUIに出さない**（撤去済み・下記§8参照）。

---

## 5. メンバータブ 仕様

- コネクト済みの全5階層を **顔グリッド** で表示（アバター＋名前1行＋階級バッジ）。「近い関係は顔で見る」思想。
- 多人数の階層（Mid / Wide / Connect）は **先頭 N人（12〜18）＋「すべて見る」** で折りたたみ。
- 顔をタップ → 操作シート:
  - **顔＋名前のヘッダーをタップ → その人のプロフィールへ遷移**（プロフ導線は顔に一本化）
  - **階層を変更**
  - **メッセージ**
  - **コネクトから外す**（赤・確認ダイアログ必須＝友達削除に相当）
  - ※「プロフィールを見る」という独立項目は置かない（顔タップがその役割を兼ねる）。並び順は 階層を変更 → メッセージ → コネクトから外す
- ヘッダーはタイトル「Circle Members」のみ（補助説明・合計人数表示は撤去済み）。
- ※初期の見本にあった「名前検索」「階層フィルタ」UIは撤去済み。本番で必要になったら改めて設計する（データはページング前提＝§8）。

---

## 6. 新規配置タブ 仕様

- 未配置の新規コネクト（例: 20人）を縦一覧。
- 各行に5つのチップ **[Inner][Near][Mid][Wide][Connect]** を並べ、**1タップで即配置＋もう一度で取り消し**。
- 顔グリッドにはせず**チップ方式**（振り分けはスピード優先という判断）。
- 見出しは「New Circle」のみ（未配置カウント・説明文・配置進捗バーは撤去済み＝§8）。

---

## 7. データ構造（見本v5／本番は実データに差し替え）

見本のダミー実装:

| 名前 | 役割 |
|---|---|
| `tiers` | 階層定義（`label` 表示名／`max` 枠上限） |
| `state.members` | 階層キーごとの配置済みメンバー配列（ダミー計386人） |
| `newPeople` | 未配置の新規コネクト（20人） |
| `state.assignments` | 新規配置タブでの割当（`Map`） |
| `renderMembers` / `renderNew` / `renderTabs` / `groupHTML` / `memberTotal` | 主要描画関数 |

本番はこれらを **実ユーザーデータ・API** に置き換える。

---

## 8. ★本番実装の申し送り（地雷・必読）

モックはデモ用の簡略実装。本番では以下を必ず対応すること。

1. **データの一本化**
   見本には歴史的経緯で Circle データが2系統ある（`circleMembers`＝4キー ／ `TIER_MEMBERS_DATA`＝5キー・connect有り）。本番は**1つのデータモデルに統合**する。

2. **多人数階層のページング／仮想スクロール**
   Wide(150) / Connect(500) は大量になる。全件一括取得・一括DOM生成は不可。**ページング or 仮想スクロール前提**で設計（サービスイン後の負荷対策）。

3. **未分類コネクトのフラグ**
   「コネクト済みだが、まだ階層未配置」の状態を表すフラグが必要（＝新規配置タブの母集団）。

4. **階層移動の永続化**
   見本の階層変更は**トースト表示だけのモック**。本番はDB更新＋楽観的UI＋失敗時ロールバック。

5. **プライバシー（片方向の私的評価）**
   配置した距離・変更履歴は **相手に通知しない／相手から見えない**。見本フッターにも明記あり:「配置した距離と変更履歴は相手には表示されません」。

6. **廃止した表示（本番でも入れない）**
   下記は大将判断で撤去済み。復活させないこと。
   - CIRCLE CAPACITY メーター（枠の埋まり具合）
   - 配置進捗（`1 / 20 配置済み`）
   - 未配置カウント／メンバー合計人数の表示

---

## 9. モック固有・本番に持ち込まない点

- 見本は**独立した単一HTML**のため、フッター(tab-bar)から本体へ **相対パス＋URLハッシュ**（`../app.html#screen-xxx`）で繋いでいる。これは**見本が別ファイルである都合**であり、**本番アプリ（統合SPA）には無関係**。本番は通常の画面遷移で実装すればよい。
- 見本上部の status-bar（`9:41 📶🔋`）と下部の tab-bar は、見本を「アプリ画面らしく見せる」ための移植。本番アプリでは元々のグローバルなヘッダー／ナビをそのまま使う。
- ダミーの人物名・スコアはデザイン確認用。実データに差し替える。

---

## 付録: 見本の変遷（参考）

`circle-triage` → `v2` → `settings-full`(縦積み・却下) → `2tab` → `v3` → `v4` → **`v5`（現行・全階層 顔グリッド＋距離軸改名）**

主な確定/変更（2026-08）:
- 2タブ構成（メンバー／新規配置）で確定
- 階層名を距離軸に改名（Core→Inner ほか）
- 見本をアプリ画面化（status-bar / tab-bar 移植）、フッター導線を追加
- ヘッダー・新規配置タブ・CAPACITYメーター等の余分な表示を撤去
